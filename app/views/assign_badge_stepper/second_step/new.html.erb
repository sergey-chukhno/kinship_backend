<%= turbo_frame_tag "new-modal" do %>
  <%= render(Ui::Modal::Header::HeaderComponent.new(title: t(".title"), back_link: new_assign_badge_stepper_first_step_path)) %>
  <div class="stepper-container mb-0">
    <div class="stepper current">
      <span class="bubble">
        <%= render(Ui::Icon::IconComponent.new(icon: "new/check", size: "16", color: "white")) %>
      </span>
    </div>

    <div class="stepper">
      <span class="bubble">
        <%= render(Ui::Icon::IconComponent.new(icon: "new/check", size: "16", color: "white")) %>
      </span>
    </div>

    <div class="stepper">
      <span class="bubble">
        <%= render(Ui::Icon::IconComponent.new(icon: "new/check", size: "16", color: "white")) %>
      </span>
    </div>

    <div class="stepper">
      <span class="bubble">
        <%= render(Ui::Icon::IconComponent.new(icon: "new/check", size: "16", color: "white")) %>
      </span>
    </div>
  </div>

  <p class="s-paragraph grey-01 mb-16px"><%= raw t(".description") %></p>

  <%= simple_form_for @assign_badge, url: assign_badge_stepper_second_step_index_path, method: :post do |f| %>
    <%= f.input :project_id,
                as: :select,
                collection: @projects %>

    <%= f.submit t('.actions.submit'), class: "button button--primary button--md button--full" %>
  <% end %>
<% end %>
