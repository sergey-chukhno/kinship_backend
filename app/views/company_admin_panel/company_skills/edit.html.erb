<%= render "company_admin_panel/layout" do %>
  <div class="admin-panel__form">
    <p class="paragraph dark-01">Compétence(s) de l'organisation</p>
    <%= simple_form_for @company, url: company_admin_panel_company_skill_path(@company), method: :put do |f| %>
      <%= render "form", f: f %>
    <% end %>
  </div>
<% end %>
