<%= render "school_admin_panel/layout" do %>
  <div class="admin-panel-member__container">
    <% if @partnerships.empty? %>
      <div class="admin-panel-member__empty">
        <p class="admin-panel-member__empty--text s-paragraph grey-02"><%= show_svg "clock.svg" %>Vous n'avez aucun partenaire dans votre établissement</p>
      </div>
    <% end %>
    <% @partnerships.each do |partner| %>
      <%= render(AdminPanelMemberCardComponent.new(partnership: @school, member: partner.company)) %>
    <% end %>
  </div>
<% end %>
